<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>  <!-- Use to insert the title dynamically -->
  <link rel="stylesheet" href="/styles.css"> <!-- External styles -->
</head>
<body>
  <!-- Blue box section at the top of each page -->
  <div class="blue-box">
    <img src="/images/Logo.gif" alt="Title" class="left-image">
    <img src="/images/Daft-Punk.png" alt="Your Photo" class="right-image">
  </div>


  <!-- The body content will be inserted here -->
  <div class="content">
    <%- body %> <!-- Content inserted from other EJS files -->
  </div>

  <style>
    .blue-box {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 80px;
      background-color: rgb(3, 3, 102);
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0 10px;
      box-sizing: border-box;
      z-index: 9999;
    }
    
    .left-image {
      max-height: 80px;
      max-width: 100%;
      object-fit: contain;
    }
    
    .right-image {
      max-height: 70px;
      max-width: 70%;
      object-fit: contain;
    }
    
    .content {
      margin-top: 80px; /* To prevent overlap with the fixed top bar */
    }
    </style>
    
</body>
</html>
